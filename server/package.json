{
  "name": "runner-arena-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API Runner Arena — soumission de course, captures, classements, TTL",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "migrate": "node scripts/migrate.js",
    "expire": "node jobs/expire-tiles.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
